import styled, { css, keyframes } from "styled-components";

const wrapper = styled.div<any>`
  height: 200px;
  background-color: rgb(242, 243, 243);
`;

const copyrigth = styled.div<any>`
  padding: 20px;
  text-align: center;
  font-weight: bold;
`;
